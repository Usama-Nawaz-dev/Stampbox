import React from 'react'
import { createNativeStackNavigator } from '@react-navigation/native-stack'

const productStack = createNativeStackNavigator();

export const ClubsStackNavigator = () => {
    return (
        <productStack.Navigator>
   
        </productStack.Navigator>
    );
};